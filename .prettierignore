# The library owns its own formatting and its own prettier version. Reformatting it from here
# would show up as noise in the submodule's diff and be reverted by its next `pnpm format`.
submodules/

node_modules/
build/
.gradle/

# npm rewrites these with its own indentation whenever the tree changes, so formatting them
# means `prettier --check` goes red on the next `npm install`. They are npm's files, not ours.
package-lock.json
tools/preview/package-lock.json

# Same argument, different tool: Claude Code rewrites this in 2-space JSON every time a command
# is approved, and this repo's prettier wants tabs. Formatting it fixes nothing - the next
# approval puts it back and takes `npm run verify` down with it.
.claude/settings.local.json

# Generated, both of them.
tools/preview/dist/
watchface/src/main/res/raw/watchface.xml

# A generated snapshot, not source. It is 1402 rendering elements written by --snapshot; the
# whole point is that it changes only when the face does, so reflowing it would be a diff for
# nothing.
tools/gen/face.model.json

