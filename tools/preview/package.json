{
  "name": "redwatchface-preview",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live preview of the watch face. Deliberately isolated from the root package so the generator stays dependency-free.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.39.5",
    "svelte-check": "^4.3.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  },
  "engines": {
    "node": ">=22.18"
  }
}
