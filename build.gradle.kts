// Android Studio will offer to upgrade these; accepting is fine.
// AGP 8.10+ is the first line that officially knows about compileSdk 36.
plugins {
    id("com.android.application") version "8.10.1" apply false
}
